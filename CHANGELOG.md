# 변경 이력

호롱호롱의 버전별 변경 사항을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은 [유의적 버전](https://semver.org/lang/ko/)을 따릅니다.

각 릴리즈의 설치 파일과 전체 노트는 [Releases](https://github.com/Jungjihyuk/horong-horong/releases)에서 볼 수 있습니다.

## [Unreleased]

### 추가

- 할 일(Todo), 빠른 메모(Quick Note), 일기(Diary), 참고 자료(References), 지식/작업 볼트(Knowledge/Works Vault)를 아우르는 통합 «Second Brain(Mind)» 작업 공간을 추가했습니다.
- 성취 타임라인에 완료 여부(완료 먼저, 완료 나중)와 시간 순서(오름차순, 내림차순)를 조합해 볼 수 있는 정렬 필터 옵션을 추가했습니다.
- 메뉴바 팝오버의 메모 탭에서 완료된 항목 대신 마감일 기준 다가오는 할 일을 우선 보여주도록 개선했습니다.

### 변경

- 앱 전반의 아키텍처를 MVVM + Clean Architecture(Presentation / Domain / Data)로 전면 개편했습니다.
  - 모든 View 계층에서 SwiftData `@Query`와 `ModelContext`를 완전히 제거(0개)하고, ViewModel과 Repository 프로토콜 기반으로 전환하여 메모 편집 시 보이지 않는 화면까지 다시 계산되던 쓰기 증폭 문제를 해결했습니다.
  - `Mind`, `News`, `Lab`, `Achievement`, `Reward`, `Timer`, `Stats` 등 모든 주요 비즈니스 기능을 ViewModel + Repository 구조로 마이그레이션했습니다.
  - 레거시 `Features/`, `Models/`, `Services/` 폴더를 완전히 소멸시키고 목표 아키텍처 폴더로 재배치했습니다.
- `FocusSession`과 `AppCategoryRule` 등 단일 파일에 여러 `@Model`과 도메인 타입이 섞여 있던 거대 모델 파일들을 1모델 1파일로 독립시키고, 순수 정책과 비즈니스 엔티티를 `Domain/` 계층으로 분리했습니다.
- 1,900여 줄의 전역 설정 파일(`Constants.swift`)을 UI 기능 순서(일반/외관 → 타이머 → 카테고리 → 트래커 → 컴패니언 → 성취/보상 → 메모 → 뉴스/AI → 저장소 키)에 맞추어 10대 섹션으로 체계화하고 가이드 주석을 보강했습니다.
- 한국어 조사 처리 도구, AI 실행 도구, 설정 스토어들을 디자인 시스템, 어댑터, 로컬 데이터 소스 계층으로 각각 알맞게 재배치했습니다.

### 수정

- 메뉴바 팝오버 등 non-activating 창을 열었을 때 앱 활성화 타이밍 문제로 한글 입력이 막히던 현상을 수정했습니다.
- 컴패니언 대화창 입력 중 말풍선이나 메뉴가 노출될 때 입력창 포커스가 해제되던 문제를 수정했습니다.
- 컴패니언 오버레이 카드가 열려 있는 동안 빈 프레임 갱신으로 인해 카드가 깜빡이던 문제를 수정했습니다.
- 일기(Diary) 실행 시 날짜별 중복 레코드가 발생하지 않도록 저장 전 중복 검증 및 병합 로직을 강화했습니다.
- 메모 텍스트 편집기에서 플레이스홀더 텍스트와 커서 위치가 어긋나던 문제를 수정했습니다.

### 제거

- 구형 독립 메모 창(`MemoBrowserWindow`)과 사용하지 않는 메모 보관(Archive) 기능을 제거했습니다.

## [0.2.9] - 2026-08-29

### 추가

- AI 모델의 실제 실행 기록을 공급자·모델·주간/월간 작업별로 살펴보고 성공 여부, 실행 방식, 입력과 파싱 결과를 확인하거나 직접 평가할 수 있는 «AI 실험실»을 추가했습니다.
- AI 실험실에 골든셋 평가 결과를 모델별·데이터 유형별로 비교하고 LLM 심사 점수, 맥락 적용 효과와 파싱 진단을 확인하는 화면을 추가했습니다.
- 월간 목표 추천에서도 MLX와 Ollama 로컬 모델을 사용할 수 있게 했습니다.
- 목표 추천에 페르소나와 프로필 맥락을 반영하고, 목표를 만들기 위한 정보가 부족하면 필요한 정보를 안내하도록 했습니다.
- Ollama 모델 선택지에 Qwen 3.8 27B와 EXAONE Deep 7.8B를 추가했습니다.
- 목표 추천의 채택·완료 여부를 기록해 추천 품질을 분석할 수 있게 했습니다.
- 목표 추천을 여러 모델과 조건으로 반복 검증하고 LLM 심사를 함께 수행하는 골든셋 평가 도구를 확장했습니다.

### 변경

- Apple Foundation Models, MLX와 Ollama의 목표 추천 흐름을 공통 `HorongAI` 파이프라인으로 통합해 프롬프트 구성, 응답 파싱, 실행 기록과 오류 처리를 일관되게 만들었습니다.
- 로컬 모델에서 주간·월간 추천을 순차 실행하고, 공급자를 바꾸거나 일정 시간 사용하지 않으면 추천 모델을 메모리에서 내리도록 변경했습니다.
- 목표 추천 제목 위로 상태 배지를 옮겨 긴 제목을 잘리지 않고 확인할 수 있게 했습니다.
- Ollama 구조화 응답의 JSON 스키마 제약과 속성 순서를 더 엄격하게 적용하도록 변경했습니다.

### 수정

- 설정에서 하루짜리 휴가의 시작일을 바꿀 때 종료일이 따라오지 않아 날짜 선택이 꼬이던 문제를 고쳤습니다.
- 메모의 시작일과 마감일이 서로 뒤집힌 상태로 저장될 수 있던 문제를 고쳤습니다.
- 시스템 시계가 뒤로 바뀌었을 때 컴패니언 애니메이션 프레임 계산으로 앱이 종료될 수 있던 문제를 고쳤습니다.
- 로컬 모델로 주간·월간 추천을 함께 실행할 때 요청이 대기열에서 시간 초과되던 문제를 고쳤습니다.
- 설치되지 않은 로컬 모델과 모델 서버 연결 실패를 구분하지 못해 잘못된 오류를 보여주던 문제를 고쳤습니다.
- 모델 응답의 중첩 ID 배열과 `<think>` 태그 때문에 목표 추천 파싱이 실패하거나 앱이 종료될 수 있던 문제를 고쳤습니다.
- 정보 보완 안내가 관련된 모든 메모에 적용되지 않던 문제를 고쳤습니다.
- AI 실험실에서 주간·월간 실행의 평가가 서로 덮이거나 골든셋 실행이 실사용 기록에 섞이던 문제를 고쳤습니다.

### 제거

- 골든셋 평가 도구의 Gemini CLI 공급자 지원과 사용하지 않는 옛 비교 화면·초안 스크립트를 제거했습니다.

## [0.2.8] - 2026-08-11

### 추가

- 주간 목표를 달성할 때마다 «보상 받기»로 포인트를 적립하고, 월간 목표를 달성하면 그 포인트로 미리 정해 둔 보상을 골라 받는 보상 시스템을 추가했습니다. 성취 창에 «보상» 탭이 생겼습니다.
- 쌓인 포인트를 호롱불 등잔에 기름이 차오르는 모습으로 보여줍니다. 등잔에는 보상별 필요 포인트가 눈금으로 표시되고, 월간 목표를 달성해야 불이 켜집니다.
- 받고 싶은 보상을 직접 정해 두는 «보상 목록»과, 지금까지 받은 보상을 늘어놓는 «받은 보상» 선반을 추가했습니다.
- 보상을 받을 때 상자를 여는 연출을 추가했습니다.
- 적립·사용 이력을 별도 창에서 볼 수 있도록 했습니다.
- 설정 > 성취에 주간 목표 하나당 적립할 포인트를 정하는 항목을 추가했습니다. 기본값은 10P 입니다.
- 전체 메모·뉴스 리포트 보관함·통계·성취를 창 네 개로 따로 열지 않고, 왼쪽 탭으로 오가는 통합 창 하나에서 볼 수 있게 했습니다.
- 성취 타임라인에서 동그라미를 눌러 메모에 연결된 할 일을 바로 완료 처리할 수 있게 했습니다.
- 할 일 체크를 잘못해 이미 받은 주간 보상을 되돌릴 수 있게 했습니다. 이미 쓴 포인트는 되돌리지 않습니다.

### 변경

- 목표 만들기를 세 층(단계·이름 / 하위 목표·할 일 / 세부 설정)으로 재편했습니다. 제목만 적어도 목표를 만들 수 있고, 하위 목표 입력 한 줄이 기존 목표 찾기와 새 목표 만들기를 함께 처리해 위에서 아래로 세우는 방식과 아래에서 위로 묶는 방식을 모두 지원합니다.
- 목표 관리 창에서는 하위 목표를 새로 만들지 않고 연결만 다루도록 정리했습니다. 하위 목표 행의 «연결 해제»는 목표를 지우지 않고 연결만 끊습니다.
- 목표 관리 창에서 자유 입력 방식의 «보상» 항목을 없애고, 그 자리를 «기존 주간 목표 연결» 목록에 내줘 한 번에 더 많이 보이도록 했습니다.
- 성취 요약의 «지급 대기»를 «받을 포인트»로 바꿔, 지금 실제로 받을 수 있는 포인트를 보여줍니다.
- 성취 타임라인 필터 «보상만»을 «남은 것»으로 바꿨습니다. 이름과 달리 미달성 목표를 거르는 필터였습니다.
- 성취 타임라인의 기본 필터를 «전체»로 바꿨습니다.
- «성취 상세» 버튼 크기를 통계·뉴스 탭의 같은 자리 버튼과 맞췄습니다.

### 수정

- 연결된 할 일이 하나도 없는 빈 목표가 달성한 것으로 잡혀 보상 받기 버튼이 켜지던 문제를 고쳤습니다.
- 통합 창 왼쪽 탭 목록이 글자에 정확히 대야만 눌리던 문제를 고쳐, 칸 전체를 누를 수 있게 했습니다.
- 설정의 MLX 모델 목록에서 이미 받아 둔 모델에 완료 표시와 삭제 버튼이 나타나지 않던 문제를 고쳤습니다.
- 루미롱 설정에서 MLX 모델을 바꿀 때 이전 모델이 메모리에 남던 문제를 고쳤습니다.

### 제거

- 접근 권한이 있어야 해서 내려받을 수 없던 루미롱 MLX 모델(EXAONE 3.0 7.8B, EEVE-Korean 10.8B)을 목록에서 제거했습니다.

## [0.2.7] - 2026-08-10

### 추가

- 앱 설정의 AI 모델 선택기(Ollama, MLX)에 사용자가 직접 원하는 모델을 찾아 추가할 수 있는 커스텀 모델 검색 기능을 도입했습니다.
- 커스텀 모델 이름 입력 시 Ollama 레지스트리 및 HuggingFace API와 실시간으로 통신해 설치 가능 여부를 사전에 자동 검증하도록 했습니다.
- 로컬 모델 다운로드 중 언제든지 즉시 취소(Ollama 프로세스 강제 종료 및 MLX 중단)할 수 있는 기능을 추가했습니다.
- 앱 내부 AI 에이전트 연동용으로 Hermes(Python) 에이전트를 추가했습니다.
- 성취 목표에서 다른 페르소나의 하위 목표를 연결할 수 있도록 크로스 페르소나 목표 연결 기능을 추가했습니다.
- 메모의 소요 시간을 사용자가 커스텀하게 설정한 포모도로 타이머 시간과 자동으로 동기화하는 기능을 추가했습니다.
- 스크린샷 캡처 대상에 컴패니언 일정(Schedule)과 뉴스 리포트 보관함(Archive)을 추가했습니다.

### 변경

- 기존 팝업 메뉴 형태였던 Ollama/MLX 모델 선택기 UI를 카드 목록형태(페이지네이션 포함)로 통합 개편했습니다.
- 성취 탭의 여정(Journey) 뷰 시각화를 개선하고 목표 깃발(Flag) 정렬 로직을 고도화했습니다.
- 앱 내 각종 버튼 및 고급 설정 항목들의 클릭 가능 영역(Clickable Area)을 더 넓혀 조작 편의성을 개선했습니다.
- 설정 화면의 사이드바 메뉴 그룹을 4단계(4-tier) 구조로 재편성하여 접근성과 가독성을 높였습니다.

### 수정

- 자정에 날짜가 넘어갈 때 발생할 수 있는 Actor Isolation 관련 크래시(비정상 종료) 문제를 해결했습니다.
- '오늘 할 일 계획 알림' 옵션 선택 시 UI가 백그라운드 스레드에서 업데이트되려던 문제를 고쳐, 안전하게 메인 큐에서 동작하도록 수정했습니다.
- 달력(Calendar) 화면 렌더링 시 발생하던 간헐적인 UI 오류를 수정했습니다.

### 제거

- 사용 빈도가 낮고 유지보수가 어려운 Gemini 기반 기능 및 관련 설정 UI를 모두 제거했습니다.

## [0.2.6] - 2026-08-06

### 추가

- 설정 > 외관에 테마별 수동·자동 강조 색상 선택 기능을 추가했습니다.
- UI 요소 간격을 조절하는 인터페이스 밀도(여유 있게·기본·조밀하게) 설정 옵션을 추가했습니다.
- 설정 > 외관에서 변경 가능한 다양한 앱 아이콘 스타일 선택 기능을 추가했습니다.
- 팝오버 토글 및 타이머 제어를 위한 전역 단축키 사용자 지정 기능을 추가했습니다.
- 지정한 주기에 맞춰 뉴스를 자동 생성하는 리포트 스케줄러 및 설정 > 뉴스 관리 UI를 추가했습니다.

### 제거

- 외관 설정의 미사용 메뉴바 애니메이션 옵션을 제거했습니다.
- 뉴스 공급자 목록 및 실행 스크립트·Makefile에서 미사용 Gemini 옵션을 제거했습니다.

## [0.2.5] - 2026-08-05

### 추가

- 포모도로 진행 중 최근 10분의 몰입도와 앱 전환 횟수로 흐트러짐을 감지하고, 호로롱이가 판정 이유와 측정값을 알려주는 집중 넛지를 추가했습니다.
- 설정 > 몰입에서 집중 넛지의 판정 방식·기준·세션당 횟수·문구를 조정하고, 회고 기록을 바탕으로 개인별 기준을 적용할 수 있도록 했습니다.
- 통계 상세에 최근 8주의 주간 집중 넛지 횟수와 카테고리별 몰입도 추이를 추가했습니다.
- 실제 앱 사용 기록을 바탕으로 함께 몰입한 카테고리 짝을 제안하는 기능을 추가했습니다.

### 변경

- 포모도로 회고와 앱 사용 분류의 저장 단계를 분리해, 앱 분류를 나중에 하더라도 회고 응답은 먼저 저장되도록 변경했습니다.

## [0.2.4] - 2026-08-04

### 추가

- 뉴스 리포트를 앱 안에서 검색하고 읽을 수 있는 전용 보관함을 추가했습니다.
- 처음 사용하는 사용자도 사용법 안내를 따라갈 수 있도록 풍부한 예시 일정·메모·몰입 데이터를 추가했습니다.

### 변경

- 사용법 안내의 설명 순서와 단계별 강조 위치를 실제 기능 흐름에 맞게 개선했습니다.
- 뉴스 화면의 최근 리포트에서 전용 보관함을 열 수 있도록 변경했습니다.

### 수정

- 저장 위치를 변경한 뒤 뉴스 리포트 보관함이 이전 절대 경로를 참조해 일부 파일을 읽지 못하던 문제를 고쳤습니다.
- 루미롱 메뉴와 오버레이에서 버튼이 눌리지 않거나 클릭이 제대로 전달되지 않던 문제를 고쳤습니다.
- 루미롱 오버레이가 화면 가장자리에서 잘못 배치되던 문제를 고쳤습니다.
- 월간 성취 진행률이 아직 지나지 않은 주차까지 포함해 계산되던 문제를 고쳤습니다.
- 메모 알림이 시작일 대신 마감일을 기준으로 표시되던 문제를 고쳤습니다.

## [0.2.3] - 2026-08-03

### 추가

- 성취 목표 추천에 MLX 공급자 추가. 앱 안에서 직접 추론하며 Apple 모델보다 넓은 컨텍스트를 씁니다.
- 성취 목표 추천에 Ollama 공급자 추가. 가중치가 앱 메모리를 쓰지 않아 더 큰 모델을 쓸 수 있습니다.
- 설정 > 성취에 추천 엔진 선택과 모델 피커 추가. 두 공급자 모두 결과가 없으면 Apple 모델로 대신 답합니다.
- 설정에서 MLX·Ollama 모델을 직접 내려받고 지울 수 있습니다. 성취·루미롱·뉴스 세 화면 모두에서 동작합니다.
- MLX 모델 다운로드에 진행률·속도·남은 시간과 정체 감지 표시 추가.
- 루미롱 MLX 모델 후보에 Qwen3 8B 추가.
- 목표 추천 품질을 지키기 위한 골든셋 평가 하네스와 회귀 테스트 추가.

### 변경

- 루미롱 설정의 «모델 준비» 행을 없애고, 성취 탭과 같이 모델 목록의 각 행에서 받고 지우도록 통일했습니다.
- 루미롱에서 MLX 모델을 바꾸면 이전 모델을 메모리에서 즉시 내리고 새 모델을 올립니다. 아직 받지 않은 모델은 내려받지 않고 이전 모델만 내립니다.

### 수정

- 성취 주간 타임라인이 헤더에 표시된 주와 다른 주를 그려, 지난주 할 일이 이번 주에 표시되던 문제를 고쳤습니다. 주 시작 요일을 앱 전체에서 월요일로 통일했습니다.
- MLX 모델 다운로드 진행률이 14MB에서 멈춘 것처럼 보이다가 마지막에 100%로 튀던 문제를 고쳤습니다.
- 받아 둔 모델 가중치를 지워도 «받음»으로 표시되어, 추천을 실행할 때 조용히 다시 내려받던 문제를 고쳤습니다.
- HuggingFace 캐시 경로가 고정되어 있어 `HF_HOME`·`HF_HUB_CACHE` 설정을 따르지 않던 문제를 고쳤습니다.

## [0.2.2] - 2026-07-28

배포하지 않은 0.2.1의 변경 사항이 모두 포함되어 있습니다.

### 추가

- 완료된 포모도로 세션의 할 일 연결·변경·해제 기능.
- 앱 빌드별 자동 백업 생성과 최근 5개 백업 보존.
- 일간·주간·월간 몰입 분석과 할 일 연계 인사이트.
- 성취 목표의 마감일과 주간 진행 기록, 타임라인에서 연결된 할 일 미리보기.

### 변경

- 운영 데이터와 디버그 데이터 저장소를 분리하고 저장소 버전 관리·자동 이전을 도입했습니다.
- 오늘 할 일을 `남은 수/오늘 전체 수` 형식으로 표시하고, 남은 할 일과 마감이 지난 할 일을 구분합니다.
- 메모 브라우저의 불필요한 렌더링과 입력 중 저장 부하를 줄였습니다.

### 수정

- 앱 업데이트 과정에서 포모도로와 연결된 할 일 정보가 바뀌던 문제.
- 목표 진행률과 순위 계산의 정확도 문제.

## [0.2.1]

태그만 만들고 배포하지 않았습니다. 변경 사항은 0.2.2에 포함되어 있습니다.

## [0.2.0] - 2026-07-01

### 추가

- **성취 목표 관리** — 주간·월간 목표를 관리하고 메모와 할 일을 목표 흐름으로 연결합니다.
- 페르소나·비전·월간 목표를 여정 형태로 시각화하고, 완료된 목표를 달성 기록에서 다시 볼 수 있습니다.
- 성취 타임라인에서 요일별 할 일을 확인하고 날짜를 조정할 수 있습니다.
- AI 기반 주간·월간 목표 추천.
- 팝오버 `와인 랜턴` 테마.

### 변경

- 픽셀 테마와 포커스 아이콘 에셋을 다듬고, 주요 팝오버 화면의 테마 적용을 개선했습니다.
- README의 기능 스크린샷을 최신 UI 기준으로 교체했습니다.

## [0.1.3] - 2026-06-01

Sparkle 자동 업데이트 서명 체인을 바로잡은 릴리즈입니다. 상세 노트는 남아 있지 않습니다.
관련 기록: [Sparkle 업데이트 서명 체인 인시던트](docs/5.%20운영/프로젝트%20운영/12.%20기술%20문서/Bug/incident-20260601-sparkle-update-signing-chain.md)

## [0.1.2] - 2026-05-31

상세 노트가 남아 있지 않습니다. [릴리즈 페이지](https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.1.2)를 참고하세요.

## [0.1.1] - 2026-05-22

상세 노트가 남아 있지 않습니다. [릴리즈 페이지](https://github.com/Jungjihyuk/horong-horong/releases/tag/V0.1.1)를 참고하세요.

## [0.1.0] - 2026-05-01

첫 공개 릴리즈.

[Unreleased]: https://github.com/Jungjihyuk/horong-horong/compare/v0.2.9...HEAD
[0.2.9]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.9
[0.2.8]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.8
[0.2.7]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.7
[0.2.6]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.6
[0.2.5]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.5
[0.2.4]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.4
[0.2.3]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.3
[0.2.2]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.2
[0.2.0]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.2.0
[0.1.3]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.1.3
[0.1.2]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.1.2
[0.1.1]: https://github.com/Jungjihyuk/horong-horong/releases/tag/V0.1.1
[0.1.0]: https://github.com/Jungjihyuk/horong-horong/releases/tag/v0.1.0
